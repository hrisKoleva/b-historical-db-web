Given the user has logged in into https://upklinge-hdb.azurewebsites.net/
When they click the "Products" scope button
Then the Products view is loaded
Then a table with Product Code, Component Code,  

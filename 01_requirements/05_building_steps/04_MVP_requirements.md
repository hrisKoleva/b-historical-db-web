WE HAVE TO DECIDE the data access approach - I don't want specific customers repository or orders repository or suppliers repository and SQL QUERIES in the code  - the requirements - the landing page is a dashboard with big cards showing the number of Customers, Suppliers, Customer Orders, Purchase Orders, Manufacturing Orders, Products. When a card is clicked, the view is changed to load the corresponding scope search criteria input fields and results. The results are paginated. Inital view is no results but also has a Show All button to load all results. When the user enters or selects a combination of the search cri Advise on the best practice but also out-of-the-box approach to dynamically building and sending the queries so there's no hardocoded QUERIES and extremely maintainable codebase 